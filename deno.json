{
  "deploy": {
    "install": "npm install --no-audit --no-fund",
    "build": "NODE_OPTIONS=--max-old-space-size=2560 NEXT_TELEMETRY_DISABLED=1 npx next build",
    "runtime": {
      "type": "static",
      "cwd": "./out"
    }
  }
}
