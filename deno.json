{
  "deploy": {
    "install": "npm install --no-audit --no-fund",
    "build": "LOW_MEMORY_BUILD=1 NODE_OPTIONS=--max-old-space-size=2048 NEXT_TELEMETRY_DISABLED=1 npx next build",
    "runtime": {
      "type": "static",
      "cwd": "./out"
    }
  }
}
